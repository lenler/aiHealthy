## 介绍
轻食AI是一个面向个人用户的健康饮食助手，核心目标是解决“饮食记录成本高、建议泛化、难以持续追踪”的问题。我在项目中负责前后端核心链路开发，重点主导前端业务体验与AI交互流程设计。项目打通了“餐食记录（手动/图片）→ AI识别热量 → 结果确认入库 → 仪表盘展示 → 个性化建议/对话咨询”的闭环。

前端采用 React + TypeScript + Ant Design + Zustand，完成登录注册、饮食分析、智能识图、聊天咨询、个人中心等模块，并通过请求层封装与拦截器实现 token 注入、异常统一处理和 API 模块化，提升可维护性与联调效率。在识图链路中，我重点优化了上传与识别体验：设计上传中状态锁、识别草稿确认、失败重试和手动录入降级，确保模型不稳定时核心流程仍可完成。

后端基于 Express + Sequelize + MySQL 提供业务支撑，并通过业务校验与联合唯一约束保障“同用户、同日、同餐次”数据一致性。AI 侧接入视觉识别、建议生成和对话能力，结合工具调用读取当日饮食记录，使回答从通用问答升级为基于用户真实数据的个性化建议。整体上，我在该项目中不仅完成了功能落地，也重点关注了数据可靠性、交互连续性和系统可扩展性。

## 问题:
### **一、核心交互与状态管理**
#### 草稿数据的持久化策略：
##### 如果用户刷新页面或意外关闭浏览器，如何保证“已识别但未确认”的草稿数据	不丢失？

**回答:**在项目里 AI 识图草稿目前仅存在组件级 state 中，刷新或关闭浏览器会丢失，没有持久化机制；清空草稿依赖用户点击“清空结果”按钮触发,如果要做这个内容可以现在将结果持久化

+ 你会选择 `localStorage`、`sessionStorage` 还是 IndexedDB？为什么？
  **回答:** 我会选择localStorage 因为草稿数据本身没有特别大 不包含图像或者大对象 而且需要在退出浏览器后还能持久化 那localStorage是最好的选择

##### 如何在 Zustand 中结合持久化中间件（persist middleware）或手动订阅机制，实现草稿的自动保存与清理（确认/取消后）？

**回答:** 在Zustand中实现持久化,先创建一个draftStore,并且可以使用persist中间件,然后使用createJSONStorage方法将结构化数据存储到localstrage中,

#### 异步流程的状态锁定与超时处理：
##### 在上传图片等待 AI 响应的过程中，如果网络超时或后端无响应，前端如何避免“永久 Loading”的死锁状态？

**回答:**目前靠 Axios 超时 + Upload 内部状态结束上传，但如果后端无响应，UI 只是报错提示，没有显式 “loading 解锁” 状态位。

##### 你是否实现了客户端的超时中断机制（如 `AbortController` 或 `Promise.race`）？

**回答:**现有代码没有实现客户端主动中断或超时控制（无 AbortController/cancel token）。

##### 当发生超时错误时，除了提示用户，是否提供了“断点续传”或“直接转入手动录入”的平滑降级方案？

**回答:** 已有"识图 → 确认入库”与“手动录入”共用同一写入接口（ createRecord / updateRecord ）

#### 路由切换与状态保持：
##### 当用户在“上传中”或“结果确认页”切换到其他路由（如查看历史记录）再返回时，当前的任务状态（Loading 或 草稿）是否应该保留？

**回答: **我把草稿数据设计为可恢复的全局状态，路由切换不会丢；上传任务启动后会在后端会将识别后的信息返回给全局状态,所以切换路由当前数据不会丢失

##### 在 React Router 架构下，如何利用 Zustand 的全局特性确保组件卸载后状态不丢失，同时避免内存泄漏？

**回答:** 先将数据提升到 Zustand同时持久化到 localStorage/IndexedDB，组件卸载后仍可恢复；“上传任务状态”可放到全局队列（taskId + status），路由切换后依然能查询到进度；组件卸载时用 useEffect 清理订阅/定时器，避免内存泄漏（例如取消轮询或上传监听）。

#### 并发控制与防抖节流：
##### 虽然 UI 层做了按钮禁用，但在网络极快或用户通过脚本操作的情况下，如何从逻辑层（Request Interceptor 或 Store Action）彻底杜绝重复提交？

**回答:**我会在节流和 UI 禁用之外，在做请求层的锁和去重，避免脚本或极端网络条件下的重复提交。在请求层，使用map数据结果，并通过“请求方法-网址“的形式动态生成key值，判断是否已经存在相同的请求操作 如果存在就拒绝请求并提示用户请勿重复提交 在响应拦截器中获取key值 将map中的数据删除掉 放置下一次请求被拦截

##### 对于图片上传这种大文件操作，是否有做分片上传或进度监控？

**回答:**没做 因为我限制了大文件操作,将图片文件限制在了5mb之下

#### 关于“上传中状态锁定”的实现：
##### 在 React + Zustand 的架构下，你是如何管理这个“锁定”状态的？是仅仅在 UI 层禁用按钮，还是在更底层的状态管理或路由守卫层面做了限制，防止用户在请求未完成时切换页面导致数据不一致？

##### 如果用户在上过程中直接关闭浏览器或刷新页面，你有什么机制来处理这种中断情况吗？

**回答**:

- 现状 ：依赖组件内部 State 和 Antd Upload 的状态，切页会中断 UI 更新但请求可能后台跑完。
- 回答话术 ：“目前我采用组件级 State 管理锁定，配合 beforeunload 拦截浏览器关闭。对于路由切换，最佳实践是将上传任务 ID 存入 Zustand 全局队列，在路由守卫中做拦截，或者将上传组件设计为全局单例（如右下角悬浮窗）以支持后台上传。

#### 关于“识别结果可选择后写入”的数据流：
+ 你提到“结果可选择”，这意味着 AI 返回的可能是一个包含多个候选项的列表，或者是一个允许用户修正的结构化数据。
+ 请描述一下从**“上传图片” -> “接收 AI 响应” -> “用户确认/修改” -> “最终写入数据库”**这一整个过程中的前端数据结构变化。
+ 在 Zustand 中，你是如何存储这些“临时草稿”数据的？如何确保用户确认前的数据不会污染正式的饮食记录状态？

**回答: **

- 现状 ：数据流是 Upload -> API -> Component State (Draft) -> User Selection -> API (DB) 。

- 回答话术 ：“我设计了独立的‘草稿区’，它与正式的‘饮食记录’完全隔离。Zustand 只存用户信息和全局目标，草稿这种临时状态留给组件级 State，既避免了 Global Store 膨胀，又保证了用户未确认前绝对不会污染正式数据。”

#### 关于“失败重试与降级”：
+ 当 AI 识别失败时，你的“重试”逻辑是简单地把原请求再发一遍，还是会根据错误类型（如网络超时 vs 服务内部错误）做不同的处理？
+ 你提到了“手动录入降级”，这个降级入口是在什么时机触发的？是自动弹出建议用户手动录入，还是由用户主动发现失败后寻找入口？在代码结构上，识别流程和手动录入流程是复用的同一套表单组件吗？

**回答:**
- 现状 ：手动重试，逻辑复用统一 API。
- 回答话术 ：“AI 识别本质上只是数据的一种‘输入源’。我的架构设计是将‘数据获取’（AI/手动）与‘数据写入’（API）解耦。当 AI 链路失败（如 5xx 错误），用户可无缝切换到手动录入模式，因为底层调用的都是同一个 createRecord 接口，这种复用性设计大大降低了降级成本。”
- 
1. **数据一致性与并发控制**
+ **问题**：如果用户快速点击“确认入库”，或者在网络波动下重复提交，前端如何防止产生脏数据？后端联合唯一索引失效时，前端如何配合？
+ **考察点**：对幂等性、竞态条件的理解，以及前后端协作保障数据一致性的能力。
+ **准备重点**：前端按钮防抖/节流、请求锁（Request Lock）、后端幂等键的设计思路。

### 技术架构与选型决策
#### 状态管理选型（Zustand vs Redux/Context）：
+ 为什么在这个项目中选择 Zustand 而不是 Redux Toolkit 或 React Context？
+ 在大型应用中，Zustand 如何避免不必要的组件重渲染？（考察 `useShallow` 或 selector 的使用）
+ 你是如何组织 Store 结构的？是按功能模块切片（Slice）还是按业务领域？

#### TypeScript 的类型安全实践：
+ 在对接 AI 接口时，返回的数据结构往往是不确定的（尤其是大模型）。你是如何定义 TypeScript 类型来保障安全性的？
+ 是否使用了 Zod 或 Yup 等库进行运行时的数据校验（Runtime Validation）？如何处理 AI 返回字段缺失或格式错误的情况？

#### 前端工程化与模块化：
+ 你提到了“API 模块化”和“请求层封装”。请具体说说你的 Axios 拦截器做了哪些统一处理（Token 注入、全局错误码映射、白名单机制等）？
+ 项目中是否有建立统一的 UI 组件库或业务组件库（如 `FoodUploader`, `CalorieCard`）？如何保证它们的复用性和可测试性？

#### 请求层封装 (9.3-Q5)
+ 你的 Axios 拦截器做了哪些统一处理？如何处理 Token 过期、全局错误码映射以及重复请求取消？
+ **考察点**：工程化能力，是否具备构建健壮网络层的能力。
+ **准备重点**：Token 无感刷新逻辑、错误统一 Toast 提示、AbortController 的使用。

### AI 业务场景的特殊挑战
这部分考察你对“AI + 前端”这一特定领域的理解。

#### AI 流式响应（Streaming）的处理：
+ 如果 AI 的建议或对话是流式输出的（Server-Sent Events 或 Chunked Transfer），前端是如何实现打字机效果的？
+ 在流式接收过程中，如果用户中途打断或发送新消息，前端如何处理未完成的流？

#### 非结构化数据的清洗与展示：
+ AI 识别出的食物名称、热量可能包含幻觉或不准确信息。前端在展示前做了什么数据清洗或格式化工作？
+ 如何设计 UI 让用户方便地修正 AI 识别错误的字段（例如：把“苹果”改成“青苹果”，热量从 50 改到 60）？

#### 全栈鉴权与数据隔离
+ “作为一个多用户系统，你是如何设计鉴权机制的？如何防止用户 A 查到用户 B 的饮食记录？”
+ **你的回答亮点（基于日志）**：
    - **登录流程**：
        * 密码加密：使用 `bcrypt` 对密码进行哈希存储，登录时比对（兼容未加密旧数据并自动升级）。
        * Token 生成：登录成功后签发 JWT， payload 中包含 `userId`。
    - **请求拦截**：前端 Axios 拦截器自动携带 Token；后端路由中间件解析 Token 获取 `userId`。
    - **数据隔离（关键点）**：
        * 在所有核心接口（聊天、饮食记录、健康信息）中，**强制从后端解析出的**** **`**userId**`** ****作为查询条件**，而不是信任前端传来的 `userId` 参数。
        * 日志中提到：“修改聊天路由 增加 userId 参数...在数据库中建立 userId 字段”，并在 `findAll` 和 `create` 时显式带入 `userId: uid`。
    - **异常处理**：对缺少 `userId` 或格式错误的情况返回 400 错误，杜绝越权访问。

#### 隐私与安全：
+ 用户上传的食物照片包含个人隐私吗？在前端上传前是否做了压缩或脱敏处理？
+ Token 是如何存储和管理的？如何防止 XSS 攻击窃取用户身份？

#### AI 识图链路的异常处理
+ 当 AI 模型识别失败、超时或输出格式不稳定时，你的前端做了什么兜底策略？如何设计“手动录入降级”流程以保证核心业务不中断？
+ **考察点**：**这是刚才我们讨论的重点**。考察系统的鲁棒性和以用户为中心的设计思维。
+ **准备重点**：详细描述“草稿箱”机制、超时自动重试、以及从“识别结果”平滑切换到“手动表单”的交互设计。

#### 关于“上下文记忆”与消息数据清洗
+ 在使用 AI SDK 时，前端传来的消息结构（含 `<font style="color:rgb(6, 10, 38);">parts</font>`、`<font style="color:rgb(6, 10, 38);">id</font>` 等）往往不能直接发给大模型。你是如何做数据清洗以维持多轮对话上下文的？

### 性能优化与用户体验
这部分考察你对极致体验的追求。

#### 首屏加载与资源优化：
+ 针对图片上传-heavy 的应用，做了哪些图片优化策略（懒加载、WebP 转换、CDN 加速）？
+ 包体积（Bundle Size）是否做过分析？有没有使用代码分割（Code Splitting）或动态导入？

#### 弱网环境下的体验：
+ 在地铁或信号差的环境下，上传失败率很高。你的重试机制是怎样的？（指数退避算法？）
+ 是否考虑过离线优先（Offline-first）策略？比如用户先本地保存记录，网络恢复后自动同步？

### 软技能与项目复盘
这部分考察你的成长思维和团队协作。

#### 最难的技术攻坚：
+ 在整个项目开发过程中，你遇到的最大的技术难点是什么？你是如何定位问题、分析方案并最终解决的？（请准备一个具体的 STAR 案例）

#### 如果重新做一次：
+ 如果现在让你重新从零开始做这个项目，基于现在的经验，你会在架构设计、技术选型或开发流程上做哪些不同的改变？为什么？

