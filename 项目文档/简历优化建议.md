简历改写（更稳、更真实、可面试）

二次封装 **Axios 请求层与拦截器**，统一 **token 注入**、**异常抛出** 与 **POST 请求去重**，拦截连点或脚本触发的重复提交；按业务域拆分 **API**，并为实时性不强的 **GET 请求接口** 增加 **短时强缓存** 与 **ETag 协商缓存**，降低高频访问压力，提升接口一致性与响应效率。

基于 **Zustand + LocalStorage** 构建**登录态**与 **AI 建议缓存机制**，支持刷新后状态恢复与按日期复用建议结果，减少重复请求并提升页面访问连续性。

设计 **AI 识图上传与结果确认链路**，引入**上传状态锁**、**草稿暂存**与**人工二次确认机制**，减少误提交与无效入库，提升识别交互的稳定性与用户操作可控性。

按识图、问答、建议三类场景接入**视觉模型**、**对话模型**与**结构化生成模型**，并通过 **Prompt 约束**、**JSON 输出控制**与**后端兜底解析**提升大模型输出稳定性，支撑识别结果展示与建议生成。

基于 **AI SDK** 与 **DeepSeek** 实现**健康问答流式输出**，并通过**工具调用**读取用户当日饮食记录，使 AI 回答能够结合真实业务数据生成个性化饮食建议。

基于 **Express + Sequelize + MySQL** 设计用户、健康档案、饮食记录与聊天记录等**核心数据模型**，并通过**业务校验**与**查询条件约束**保障**同用户同日同餐次数据一致性**。
